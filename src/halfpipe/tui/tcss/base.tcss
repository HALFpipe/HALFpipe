MainApp {
    Screen {
        align: center middle;
    }
    #welcome_image {
    width: 70;
    height: 33;
    }
    #file_browser_tab {
        align: center top;
    }
    HeaderTitle {
        text-style: bold;
    }
    Header {
        height: 1;
        background: $accent;
    }
    Tab.-active {
        text-style: bold;
        color: $text;
        background: $accent;
    }
}
